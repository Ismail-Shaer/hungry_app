import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xff08431D);

  // Payment method tile background colors
  static const selectedTileColor = Color(0xFF3C2F2F);
  static const unselectedTileColor = Color(0XFFBCBBFF);
}
